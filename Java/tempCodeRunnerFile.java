for (int i = 1; i < partitions.length; i++) {
        //     partitions[i] = partition(arr, partitions[i-1] - 1, partitions[i-1] + 1);
        // }

        // for (int partition : partitions) {
        //     System.out.println("Index: " + partition);
        // }